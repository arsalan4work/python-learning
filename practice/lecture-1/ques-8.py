# Develop a program that inputs two numbers and prints their sum. 
# Ensure to convert the input values to integers before performing the addition.

sum1 = int(input("Enter the first number: "))
sum2 = int(input("Enter the second number: "))

sum_result = sum1 + sum2

print("Sum:", sum_result)